# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# secrets / local config with tokens
.mcp.json

# personal/local instructions, not shared with the team
CLAUDE.local.md

# local dev server tracking (pid/log) from ad-hoc background `pnpm dev` runs
dev.log
dev.pid
.env*
# ...except committed templates (.env.example was already tracked before
# the blanket rule above existed, which is why it didn't need this — new
# ones do).
!e2e/.env.test.example
# Placeholder-only values for CI's build step (.github/workflows/ci.yml)
# — no real secrets, safe to commit.
!.env.ci

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
e2e/visual/**/*-snapshots/*-actual.png
e2e/visual/**/*-snapshots/*-diff.png
